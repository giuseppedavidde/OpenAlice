Liberation Sans Regular (OFL), from pdfjs-dist standard_fonts.
The unmodified TTF is base64-encoded in JSON to stay inside the Connector
bundle on headless hosts; no system fonts or native libraries are required.
